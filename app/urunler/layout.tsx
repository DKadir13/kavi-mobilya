/** Ürün listesi segmenti için ISR süresi (sunucu tarafı fetch / RSC önbelleği) */
export const revalidate = 3600;

export default function UrunlerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
